/** Generated by AWAG ver.2.0.0.GIT-e0917bf.BUILD-20170421-1451+0900 */

package com.ibm.salessample.resource.impl;

import javax.enterprise.context.Dependent;
import javax.inject.Inject;
import javax.interceptor.Interceptors;
import javax.ws.rs.NotFoundException;
import javax.ws.rs.core.Response;
import javax.ws.rs.core.Response.ResponseBuilder;
import javax.ws.rs.core.Response.Status;
import com.ibm.jp.awag2.common.logic.ServiceAppException;
import com.ibm.jp.awag2.common.logic.ServiceDBException;
import com.ibm.jp.awag2.common.logic.ServiceIF;
import com.ibm.jp.awag2.common.util.MethodTraceLoggingInterceptor;
import com.ibm.salessample.dto.GetContactListInputDTO;
import com.ibm.salessample.dto.GetContactListOutputDTO;


/**
* コンタクト履歴一覧取得 リソースクラス（GetContactList）
*
*/ 
 @Interceptors(MethodTraceLoggingInterceptor.class)
@Dependent
public class GetContactListResource {

	/** コンタクト履歴一覧取得 Service（GetContactList） */
	@Inject
	private ServiceIF<GetContactListInputDTO, GetContactListOutputDTO> service;

	public Response execute(GetContactListInputDTO input) throws ServiceDBException, ServiceAppException {
		
		// CUSTOMIZE: ビジネスロジック前処理を実装
		
		GetContactListOutputDTO result = service.execute(input);

		// CUSTOMIZE: ビジネスロジック後処理を実装
		

		ResponseBuilder rb = null;
		if (result == null) {
			rb = Response.status(Status.NO_CONTENT);
		} else {
			rb = Response.ok(result);
		}
		
	
		return rb.build();
	}
}

